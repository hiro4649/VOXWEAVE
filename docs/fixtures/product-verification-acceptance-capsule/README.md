# Product Verification Acceptance Capsule Fixtures

These fixtures are safe metadata only. They do not contain raw logs, raw
artifacts, endpoint values, token values, secret values, private paths, raw
audio, product verification output, remote diagnostic output, runtime output,
or model output.

The fixtures support the v1.1.8 Product Verification Acceptance Capsule
Implementation Candidate self-check. They are not product verification
execution evidence, runtime evidence, remote diagnostic evidence, readiness
evidence, or merge evidence.
