# IRIS Adapter Fixture-Only E2E Mock Fixtures

These fixtures are offline-only safe adapter packets for the v1.1.6 IRIS Adapter
Fixture-Only E2E Mock Candidate. They do not contain raw audio, endpoint values,
renderer endpoints, model paths, secrets, tokens, canonical envelopes, command
fields, datasets, benchmark data, or production configuration.

The fixtures are not runtime evidence, not production evidence, not real TTS
evidence, not ASR runtime evidence, not Live2D renderer evidence, and not merge
readiness evidence.
