# Owner-Scoped Runtime Return Limited Local Fixtures

These fixtures are static mock metadata for loopback-only runtime return
verification. They do not contain raw audio, endpoint values, secrets, tokens,
model paths, external URLs, or product verification inputs.
