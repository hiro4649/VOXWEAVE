# VOXWEAVE Loopback Failure and Rollback Matrix v1.2.6

## Executive Summary

This record documents the local fake-only loopback failure and rollback matrix
for VOXWEAVE v1.2.6. The matrix exercises accepted, rejected, timeout,
connection reset, and redirect-blocked fake renderer outcomes using only
independent 127.0.0.1 ephemeral servers. It emits safe JSON summaries only and
does not claim runtime or production readiness.

## Source State

- currentActiveHarness: v1.2.6
- baselineRunnerPr: #373
- matrixScope: local_ephemeral_loopback_fake_only
- acceptedStatus: pass
- rejectedStatus: pass
- timeoutStatus: pass
- connectionResetStatus: pass
- redirectBlockedStatus: pass
- rollbackStatus: pass
- externalNetworkExecutionStatus: no
- realRendererExecutionStatus: no
- rawFailureMaterialExcludedStatus: pass
- runtimeReadinessClaimed: no
- productionReadinessClaimed: no

## Matrix Cases

| Case | Fake behavior | Expected safe status | Result |
| --- | --- | --- | --- |
| accepted | fake renderer returns accepted response | accepted | pass |
| renderer_rejected | fake renderer returns rejection response | renderer_rejected | pass |
| renderer_timeout | fake renderer delays beyond bounded timeout | renderer_timeout | pass |
| connection_reset | fake renderer destroys local socket | renderer_unreachable | pass |
| redirect_blocked | fake renderer returns local redirect | renderer_unreachable | pass |

The redirect case also starts a second local sink and verifies that the sink
receives zero requests.

## Rollback Boundary

- each case starts independent loopback servers
- each case closes servers in bounded cleanup
- failure cases close servers in finally
- redirect sink closes
- timeout is bounded
- no listener remains intentionally
- no persistent process is created
- no repo artifact is generated by the runner

## Safe Output Boundary

The matrix summary uses schema
`voxweave_loopback_integration_failure_matrix_v1` and excludes:

- port
- URL
- endpoint
- error message
- stack
- body
- headers
- API keys
- raw cue
- target material

## Test Coverage Evidence

- `node --check scripts/voxweave-loopback-integration-evidence.mjs`: pass
- `node scripts/voxweave-loopback-integration-evidence.mjs --matrix`: pass
- `node --test test/server-routes.test.js`: pass
- `node --test test/live2d-forwarder.test.js`: pass

The tests verify all matrix cases, redirect sink zero requests, timeout
classification without raw error material, connection reset classification,
renderer rejection classification, all-server cleanup, exact safe summary keys,
and absence of endpoint material.

## Decision Matrix

| Decision | Value |
| --- | --- |
| currentActiveHarness | v1.2.6 |
| terminalAction | create_pr_only_then_merge_after_same_head_qg |
| matrixScope | local_ephemeral_loopback_fake_only |
| acceptedStatus | pass |
| rejectedStatus | pass |
| timeoutStatus | pass |
| connectionResetStatus | pass |
| redirectBlockedStatus | pass |
| rollbackStatus | pass |
| externalNetworkExecutionStatus | no |
| realRendererExecutionStatus | no |
| rawFailureMaterialExcludedStatus | pass |
| runtimeReadinessClaimed | no |
| productionReadinessClaimed | no |
| mergeReadiness | no |

## Safe Next Action

Add deterministic evidence fingerprint coverage so repeated safe summaries can
be compared without exposing ports, endpoints, raw payloads, raw failures, or
private execution material.
